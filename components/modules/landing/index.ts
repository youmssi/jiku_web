// Landing module — public marketing page, FR (default, at `/`) + EN (`/en`).
// Routes render <LandingPage/>; every word lives in the content contract.
export { LandingPage } from "./landing-page";
export { LANDING_CONTENT } from "./content";
export type { LandingContent, LandingLocale } from "./content";
