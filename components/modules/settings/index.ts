// Settings module — the organizer's settings screen. It loads the session's
// organization data, so it only renders on the server; its sections are internal.
export { SettingsView } from "./settings-view";
